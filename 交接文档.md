# 项目交接文档（蜘蛛侠桌面特效）

> 本文档供新对话窗口接手使用。**开始前先读这个文件**，并确认项目路径与环境。
> **2026-08-24 更新**：网页版（抖音聊天.html）已按用户要求移除；`work/`、`frames/`、`frames_png/`、`person_sheet.png` 等中间产物已清理；交付物为 `蜘蛛侠特效\`（exe）与 `桌面特效\`（源码）。

---

## 0. 最重要的事

- **项目实际位置**：`D:\常用仓库\deepseek harness仓库\mj`
  - （原位置 `D:\deepseek harness仓库\mj` 已被清空，不要在那里找）
- **当前会话工作区**：`D:\常用仓库\deepseek harness仓库\mj应用程序`（用户已把文件复制到此，**写文件以此为准**）
- **本机 pwsh 沙箱后端不可用**（windows-acl-run 报 --temp 不存在），所有 pwsh 命令必须带 `sandbox_permissions: danger-full-access`
- **网络**：外网直连不通（github、pypi 都连不上）；GitHub 必须走代理 `http://127.0.0.1:7897`（经常失效，失效就重试或换 token URL）；pip 一律清华镜像 `-i https://pypi.tuna.tsinghua.edu.cn/simple`
- **长任务纪律**：可能超过 1 分钟的操作先报「目标/规模/预估时间」，脚本内每 10 帧/10% 打印进度；超过 2 分钟无输出视为卡死要终止换方案

## 1. 项目概况

蜘蛛侠桌面特效应用（窗口版），交付物：

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| exe 正式版（推荐） | `蜘蛛侠特效\` 文件夹 | PyInstaller onedir 打包，双击 exe 即用（含 `_internal` 素材，约 90MB） |
| 源码版 | `桌面特效\` 文件夹 | Python 源码（.pyw）+ frames + audio + icon + bat |

**GitHub**：`https://github.com/f20880479-lab/mj`（main 分支，最新提交 `480f3d5`；桌面特效新功能尚未推送）
**本地 git**：新位置自带 `.git`，历史完整（f526a0b → 1829cbc → 54c122a → 480f3d5），origin 已配置，http.proxy 已配

## 3. 桌面特效应用（桌面特效\）

- **功能**：全局键盘监听（pynput），输入 mj → 屏幕中央**最顶层**播放透明蜘蛛侠 GIF + 声音（winsound），播放完消失；窗口点击穿透；输入 `stopmj` 退出
- **特点**：pythonw 常驻约 30MB；单实例保护（CreateMutexW，重复启动自动退出）；toast 启动提示
- **置顶实现**：tkinter `-topmost` 不可靠，用 Win32 `SetWindowPos(hwnd, HWND_TOPMOST, ..., SWP_NOACTIVATE|SWP_SHOWWINDOW)` 在每次触发时强制置顶
- 文件：`蜘蛛侠桌面特效.pyw`（主程序）、`frames\`（166 张透明 PNG，f0012.png~f0177.png，约 22MB）、`audio.wav`、`启动特效.bat`、`安装依赖.bat`（pip 清华镜像装 pynput + pillow）、`使用说明.txt`、`effect.log`（运行日志）、`dist\蜘蛛侠特效\`（**PyInstaller 打包的 exe，免安装分发**）
- **exe 打包命令**（在 桌面特效 目录）：
  `pyinstaller --noconfirm --windowed --name 蜘蛛侠特效 --icon icon.ico --add-data "frames;frames" --add-data "audio.wav;." 蜘蛛侠桌面特效.pyw`
  `icon.ico` 由 PIL 从 `frames/f0040.png` 生成（多尺寸 16~256）；PyInstaller 6.x 会把数据打进 `_internal`，主程序 frozen 路径用 `sys._MEIPASS` 已处理；分发时整个 `dist\蜘蛛侠特效\` 文件夹打包（约 89MB，含 numpy/tk 依赖）
- **测试方法**：`python 蜘蛛侠桌面特效.pyw --test` 或 `蜘蛛侠特效.exe --test`（2.5 秒后自动触发）；看 effect.log 确认 `app started` / `effect trigger`
- **透明实现**：Win32 `UpdateLayeredWindow` 逐像素 alpha（参考 desk-pet 项目方案）——不用 tkinter 的 `-transparentcolor` 色键（在 >100% DPI 缩放下色键会整窗透明）。窗口句柄必须用 `win.wm_frame()`（winfo_id 是子窗口，UpdateLayeredWindow 作用于子窗口无效）。显隐用"全透明帧 vs 动画帧"控制，**不要用 ShowWindow(SW_HIDE)**（隐藏后 UpdateLayeredWindow 报错 87）
- 依赖：Python 3.11 + pynput（已装）+ tkinter（自带）
- **注意**：bat 文件用 UTF-8 会乱码（cmd 按 GBK 读），`启动特效.bat` 用通配符 `for %%f in ("%~dp0*.pyw")` 避免写中文路径

## 4. 抠图与素材管线（核心技术）

**原视频**：`8月21日.mp4`（720×720，60fps，3.3 秒，198 帧；人物是**倒挂的 Q 版蜘蛛侠**，背景纯色 RGB≈(238,238,238)）

| 脚本 | 作用 | 产物 |
| --- | --- | --- |
| `scripts/segment_solid.py` | 抠图：颜色距离(<30) + 四边泛洪去背景 + **不做口袋清除** + 1px 侵蚀 + 硬 alpha（实心）+ 边缘背景色反污染 + **BGR→RGB 转换** | 输出到 `%TEMP%\mj_frames\`（166 帧透明帧 434×720），需复制到 `桌面特效/frames/` |
| `scripts/make_sheet.py` | 30fps 抽帧(83帧) → WebP 精灵图（网页版用，已随网页版移除） | `work/sheet_q75.webp`（已清理） |
| `scripts/make_gif.py` | 166 帧 → 多帧 GIF（**仅存档，主程序已不用**） | `work/spiderman_multi.gif`（已清理） |
| `scripts/make_single_gifs.py` | 166 帧 → 166 个单帧 GIF（**仅存档，主程序已不用**） | `work/single_gifs/`（已清理） |
| ffmpeg（tools 内置） | 提取音频 0.2s~2.95s | `桌面特效/audio.wav`（已就位） |

> **桌面特效素材**：`桌面特效/frames/` 已内置 166 张透明 PNG（f0012.png~f0177.png，约 22MB），主程序用 PIL 逐帧读取 + `UpdateLayeredWindow` 显示（**不用 GIF/精灵图/色键**）。换视频时：替换 `8月21日.mp4` → 跑 `segment_solid.py` → 把 `%TEMP%\mj_frames\*.png` 复制到 `桌面特效/frames/`（注意：临时目录每进程不同，必须复制到项目内）。

**血泪教训（必须遵守）**：
1. **白色大眼睛是蜘蛛侠面具的一部分**，是"封闭的类背景色区域"——**绝对不能做"内部口袋清除"**（之前把 9000+ 像素的眼睛当手臂空隙删了，用户连续两次反馈"眼睛被扣掉"）
2. **cv2 是 BGR**，用 PIL 保存前必须 `rgba[:, :, [2,1,0,3]]` 转 RGB，否则红蓝互换（红色战衣变蓝）
3. cv2.imread/imwrite **不支持中文路径**（静默失败/读不出），中文路径用 PIL
4. 不要用 MORPH_CLOSE 填充（会把手指/脚部/蛛丝边的凹槽填成白色毛边）
5. 本 ffmpeg 的 libvpx **不支持编码 alpha**（VP8/VP9 都丢透明，实测过），透明视频用精灵图/GIF + canvas/Tk 方案（桌面端已改为 PNG 帧 + UpdateLayeredWindow）
6. 临时目录 `%TEMP%\dsh-xxxx\` 每进程不同！跨进程传文件要复制到项目内（如 `桌面特效/frames/`）

**重建流程（换视频时）**：替换 `8月21日.mp4` → 跑 `segment_solid.py` → 复制 `%TEMP%\mj_frames\*.png` 到 `桌面特效/frames/` → 重新打包 exe（如需）

## 5. GitHub 维护

- 远程：`origin https://github.com/f20880479-lab/mj.git`（main）
- 提交历史：f526a0b（初始）→ 1829cbc（用户网页端改的 README）→ 54c122a（实心抠图修复）→ 480f3d5（图标触发蜘蛛侠模式）
- 推送：本地已配 http.proxy=127.0.0.1:7897；`git push` 会弹认证失败（沙箱无交互），用一次性 URL：`git push https://x-access-token:<token>@github.com/f20880479-lab/mj.git main`
- **token 由用户每次提供**（之前给过两次，均建议用户用完即撤销；新会话需重新向用户要）
- **待推送**：桌面特效应用（新功能，尚未 commit/push）；提交时注意 .gitignore 已忽略 tools/、scripts/、.dsh-vision-toolkit/ 等中间产物
- 推送失败常见：代理失效（SSL_ERROR_SYSCALL，重试/换端口）、远端有新提交（先 fetch 看差异，rebase 合并；rebase 遇 README 冲突保留自己的完整版；编辑器问题用 `-c core.editor=:` 跳过）

## 6. 已知问题 / 注意事项

1. **（2026-08-23 已修复）桌面特效"只有声音没有图像"**：根因是 tkinter `-transparentcolor` 色键在 **>100% DPI 缩放的屏幕上失效**（窗口被虚拟化缩放，颜色插值后色键匹配错乱 → 整窗透明）。**修复**：改用 Win32 `UpdateLayeredWindow` 逐像素 alpha（参考 desk-pet），启动时先 `SetProcessDpiAwareness(2)`，句柄用 `win.wm_frame()`，显隐用全透明帧。**注意：修复后必须重启特效进程**（旧实例还在跑旧代码）
2. **（2026-08-23 已修复）声画不同步**：动画原来 30fps 播 5.5 秒（音频 2.75 秒）。原视频 60fps，已改为 **FPS=60**（166 帧 = 2.77 秒，与音频同步）
3. **注意**：不要用 `ShowWindow(SW_HIDE)` 隐藏特效窗口——隐藏后 `UpdateLayeredWindow` 报错 87（ERROR_INVALID_PARAMETER）；显隐用"全透明帧 vs 动画帧"控制
4. **交互规则（用户确认的）**：① 输入 `mj` 后**按回车**才触发（on_press 检测 Enter，typed 以 mj 结尾才 q.put('go')）；`stopmj` 同样回车退出。② 蜘蛛侠出现在**屏幕正上方**（`WIN_Y = 20`，水平居中）。③ **持续生效**：应用常驻，无论触发多少次都重播——用 `anim_id` 代次递增打断旧动画（旧 step 链检测 `my_id != anim_id` 退出），触发开头先 `layered.update(BLANK)` 清画面 + `winsound.PlaySound(None, SND_PURGE)` 停旧声音，再从头播放。④ **窗口模式**：主控制窗口有"特效开关"按钮（`enabled_flag` 普通布尔，pynput 线程安全读取），关闭窗口（X）即退出程序（`root.protocol('WM_DELETE_WINDOW', on_close)` → listener.stop + root.quit）；窗口用 `root.iconbitmap(icon.ico)` 设置图标（icon.ico 随 --add-data 打进素材）
5. 桌面特效动画帧 21.8MB（166 张 PNG），PIL 逐帧读取实测平均 10.3ms/帧（60fps 预算 16.7ms，流畅）；若卡顿可改小图（resize 到 217×360）
6. 清理 pythonw 进程时**只杀自己启动的 PID**，别 `Stop-Process -Name pythonw` 误杀用户其他 Python GUI 程序（之前误杀过一次）
7. 素材依赖链：改帧后必须同步重新生成 PNG 帧（`segment_solid.py` → 复制到 `桌面特效/frames/`）并重新打包 exe

## 7. 下一步可能的工作（未完成）

- [ ] 桌面特效应用提交并推送到 GitHub（含 README 更新）
- [ ] 桌面特效增强：托盘图标、开机自启、触发词配置界面、多屏适配

---
*交接时间：2026-08-23 编写（会话完成时）*
