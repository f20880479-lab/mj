# 蜘蛛侠桌面特效（Spiderman Desktop Effect）

一个 Windows 桌面小应用：全局监听键盘，输入 `mj` 后按回车，屏幕正上方播放透明蜘蛛侠动画（带声音），播放完自动消失。

## 使用

- **免安装**：从 Releases 下载 exe 直接使用（无需安装 Python）
- **源码运行**：Python 3 安装 `pynput`、`pillow` 后运行 `桌面特效/蜘蛛侠桌面特效.pyw`
